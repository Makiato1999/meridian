我们只保留微信登陆的部分，但是把它写成抽象，适用于接入其他方登陆的那种（比如接入小红书的登陆），语音的部分，我们暂时（不保留声音），只存转写的结果（也就是文本），但是语音源文件的字段留着，下一阶段，我们会考虑给ast转录失败的语音进行重试机制，第一阶段先不做（此外，数据库怎么存语音？什么格式）




后端方案 Neon	
免费额度 约 0.5 GB / project，免费计算额度，闲置自动休眠	
非常纯粹的 Serverless PostgreSQL，直接给连接串

